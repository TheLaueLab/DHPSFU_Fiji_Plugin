# A single .jar file can contain multiple plugins, specified in separate lines.
#
# The format is: <menu>, "<menu label>", <class name>
#
# If something like ("<arg>") is appended to the class name, the setup() method
# will get that as arg parameter; otherwise arg is simply the empty string.

Plugins>DHPSFU, "DHPSFU", plugins.uk.ac.cam.dhpsfu.DHPSFU
Plugins>DHPSFU, "DHPSFU-MultiBeads", plugins.uk.ac.cam.dhpsfu.MultiBeadsDHPSFU
Plugins>DHPSFU, "Overlay", plugins.uk.ac.cam.dhpsfu.OverlayResults
Plugins>DHPSFU, "Load File Localisations", plugins.uk.ac.cam.dhpsfu.LoadLocalisationFIle
Plugins>DHPSFU, "View and Save localisations", plugins.uk.ac.cam.dhpsfu.ResultManager
